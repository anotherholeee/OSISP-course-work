+==============================================================================+
|              Диаграмма классов — полный перечень элементов                   |
+==============================================================================+

НА ДИАГРАММЕ (все элементы проекта)
==============================================================================

Модули исходного кода (.c):
  trash_util.c   — все 9 функций (main + static)
  trash_hook.c   — все 12 функций (экспорт + static)
  trash_common.c — все 13 функций (экспорт + static)

Заголовок:
  trash_common.h — макросы, struct trash_entry, объявления API

Структура данных:
  trash_entry    — все 10 полей struct

Типы (в trash_hook.c):
  unlink_fn_t, unlinkat_fn_t

Артефакты сборки:
  libtrashhook.so, trash-util

Итого: 9 блоков на диаграмме, раскладка рядами (не вертикальная стопка).
